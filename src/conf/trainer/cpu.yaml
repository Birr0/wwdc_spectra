defaults:
  - default

accelerator: cpu

min_epochs: 1
max_epochs: 5

deterministic: warn  #- true/or no setting breaks for flows due to non-implemented deterministic operations.

gradient_clip_val: 1.0
gradient_clip_algorithm: norm
